## 1. Resolución del alias de salida

- [x] 1.1 Añadir un método privado en `AbsReadRepository` que, dado el `_config.DefaultOrderBy`, lo divida en términos por coma y, para cada término, resuelva su referencia de columna al alias de salida buscando en `_config.SelectColumns` la entrada cuya expresión cruda coincide (insensible a mayúsculas), conservando la dirección (`ASC`/`DESC`) y dejando el término tal cual si no hay coincidencia; verificar con un test unitario que resuelve correctamente tanto un `DefaultOrderBy` simple sin alias (`"nombre ASC"`) como uno compuesto con alias de tabla (`"g.fecha DESC, g.id DESC"` → `"Fecha DESC, Id DESC"`)
  - Hecho como `BuildRawExpressionToAliasMap` + `ResolveOrderByForDerivedTable` en `AbsReadRepository.cs`. Nota: no hay un seam para tests unitarios de estos métodos privados (la clase no tiene ninguno hoy; todo su SQL se verifica vía los tests de integración con Docker/MySQL de `AbsReadRepositoryMySqlTests`) — la verificación de ambos casos (simple y compuesto con alias) se cubre ahí en su lugar (ver 3.2/3.3), no con un test unitario aislado.
- [x] 1.2 Verificar que el método maneja el caso `_config.DefaultOrderBy` vacío/null (fallback actual a `{tablePrefix}fecha_creacion DESC`, que ya es válido como alias de salida al no depender de alias de tabla) sin lanzar excepción

## 2. Reescritura de GetRecentAsync

- [x] 2.1 Reestructurar `GetRecentAsync` para envolver la consulta de selección actual (`WHERE` + filtros extra + `ORDER BY {tablePrefix}fecha_creacion DESC` + `LIMIT @limit`, sin cambios) como subconsulta, y aplicar como `ORDER BY` externo el resultado del método de 1.1; verificar que el proyecto compila sin warnings nuevos
- [x] 2.2 Confirmar que `GetPagedReadModelsAsync` y `SearchForAutocompleteAsync` no se ven afectados (siguen usando `GetDefaultOrderBy()` sin cambios) revisando el diff

## 3. Cobertura de tests de integración

- [x] 3.1 Ajustar los datos de seed usados por `GetRecent_OrdenaPorFechaCreacionDescYRespetaElLimite` (en `AbsReadRepositoryMySqlTests` / `GastoTestInfra`) para que, entre los elementos seleccionables, el orden por recencia y el orden por `DefaultOrderBy` difieran, de modo que el test distinga ambos criterios
  - Desviación deliberada: en vez de tocar el seed de `gastos` (compartido por 8+ tests existentes que afirman recuentos/orden exactos, p. ej. `GetPagedReadModels_PaginaYCuentaBien` cuenta 4 filas totales), se añadió una tabla de test independiente (`productos_test`, sin JOIN, patrón "catálogo") con su propio repositorio (`ProductoTestInfra.cs`) y datos donde recencia y orden alfabético divergen a propósito. El test sobre `gastos`/`GastoReadRepository` se conserva (renombrado, ver 3.2) para seguir cubriendo el caso con alias de tabla + `ORDER BY` compuesto, pero en ese dataset recencia y `DefaultOrderBy` coinciden (no se podía hacer divergir sin romper los otros tests de `gastos`).
- [x] 3.2 Renombrar y reescribir el test a `GetRecent_SeleccionaPorRecenciaYDevuelveOrdenadoSegunDefaultOrderBy` (o nombre equivalente), verificando en el mismo test: (a) se seleccionan exactamente los N elementos con `fecha_creacion` más reciente, excluyendo otros que precederían en `DefaultOrderBy`, y (b) el resultado devuelto respeta el orden de `DefaultOrderBy` del repositorio de prueba (compuesto, con alias de tabla), no el de `fecha_creacion`
  - Renombrado a `GetRecent_SeleccionaPorRecenciaYDevuelveOrdenadoSegunDefaultOrderBy_ConAliasDeTabla` (prueba (a) y confirma que el alias no rompe la consulta; (b) queda demostrado en el nuevo test `GetRecent_SeleccionaPorRecenciaYPresentaOrdenAlfabetico` sobre `productos_test`, ver 3.1).
- [x] 3.3 Añadir un caso de test (puede ser el mismo repositorio de prueba con un `DefaultOrderBy` de una sola columna sin alias, o un segundo fixture) que cubra explícitamente el escenario de la spec `recent-query` "Repositorio con JOIN y alias de tabla ordena sin error", confirmando que no se produce un error de columna/alias desconocido
  - Cubierto por el test renombrado de 3.2 (usa `GastoReadRepository`, con alias de tabla `g` + JOIN `c` + `DefaultOrderBy` compuesto) y, para el caso sin alias, por `GetRecent_SeleccionaPorRecenciaYPresentaOrdenAlfabetico` sobre `ProductoReadRepository`.
- [x] 3.4 Ejecutar `dotnet test SergioIzq.DotnetKernel.sln --configuration Release` y verificar que toda la suite pasa, incluyendo el resto de tests de `AbsReadRepositoryMySqlTests` que no deben verse afectados
  - `dotnet build` sin errores/warnings nuevos. `dotnet test`: 6 superados, 15 omitidos (Docker no disponible en este entorno — incluye los 3 tests de `GetRecent*` y el resto de `AbsReadRepositoryMySqlTests`, que no cambian su comportamiento respecto a antes de este cambio), 7 fallos preexistentes en `main` sin relación con este cambio (`DomainEventDispatchTests`/`KernelDbContextConventionTests`, EF Core/SQL Server — confirmado reproduciendo el mismo fallo con `git stash` antes de este cambio). **Las aserciones de `AbsReadRepositoryMySqlTests` no se han ejecutado realmente contra MySQL en esta sesión** por falta de Docker local; el gate de `publish-nuget.yml` en CI sí lo hará antes de publicar.

## 4. Cierre

- [x] 4.1 Revisar `README.md` / notas de versión del paquete `SergioIzq.Infrastructure.Kernel` y añadir una entrada documentando el cambio de comportamiento de `GetRecentAsync` (marcado como incompatible/breaking para quien lo consuma), si el repositorio mantiene un changelog o sección equivalente
  - No aplica: el repositorio no mantiene `CHANGELOG.md` ni una sección de notas de versión (versionado automático vía Nerdbank.GitVersioning, sin changelog manual) — condición de la tarea no se cumple, se omite deliberadamente en vez de crear una sección nueva no solicitada.
- [x] 4.2 Ejecutar `openspec validate recent-ordenado-alfabeticamente --strict` y corregir cualquier aviso antes de dar la tarea por completa
  - `openspec validate recent-ordenado-alfabeticamente --strict` → "Change 'recent-ordenado-alfabeticamente' is valid"
